@page
@model TrabajoRazorPagesICZ.Pages.Plantas.IndexModel
@{
}


<h2>Bienvenido Usuario a tu modulo de plantas</h2>

<a asp-area="" asp-page="CrearPlanta" class="link-info form-control">Crear Planta</a>


@foreach (var planta in Model.Plantas)
{
    <div class="card" style="width: 18rem;">
        <img src="@planta.Foto" class="card-img-top" alt="@planta.NombreComun">
        @if (!string.IsNullOrWhiteSpace(planta.Foto))
        {
            <img src="@planta.Foto" class="card-img-top" alt="@planta.NombreComun" />
        }
        else
        {
            <img src="~/images/planta.jfif" class="card - img - top" alt="@planta.NombreComun" />
        }

        <div class="card-body">
            <a asp-area="" asp-page="DetallesPlantas" asp-route-id="@planta.PlantaId">
                <h3 class="card-text">@planta.NombreComun</h3>
            </a>

            <a asp-area="" asp-page="EditarPlantas" asp-route-id="@planta.PlantaId" class="link-info form-control">Editar</a>
            <h5>@planta.NombreCientifico</h5>
        </div>
    </div>
}
